{
    "viewModel": "tenant",
    "filters": [
        { "filterType": "W", "fieldName": "tenantId" },
        { "filterType": "W", "fieldName": "name" }
    ],
    "fields": [
        "tenantId", "name", "isActive", "timeZone",
        "permissions.logLevel", "permissions.logLevelErrors",
        "permissions.validFrom", "permissions.validTo"
    ]
}
