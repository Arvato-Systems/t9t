{
    "viewModel": "voiceApplication",
    "filters": [
        { "filterType": "E", "fieldName": "tenantId" },
        { "filterType": "W", "fieldName": "applicationId" }
    ],
    "fields": [
        "tenantId", "applicationId", "provider", "name", "isActive", "apiKey", "sessionPerExtSession", "providerId"
    ]
}
